export default function formatNumber(number: number, min?: number, max?: number): string {
  let fmt = "";

  //   if (min) {
  //     if(number >= min) {
  //         fmt=number.toString()
  //     } else {

  //     }
  //   }

  return fmt;
}
